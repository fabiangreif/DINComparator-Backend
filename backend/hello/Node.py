class Node:

    def __init__(self, number, title, size, color, is_reference, tokens, pagerank, similarity, most_common_number):
        self.number = number
        self.title = title
        self.size = size
        self.color = color
        self.is_reference = is_reference
        self.tokens = tokens
        self.pagerank = pagerank
        self.similarity = similarity
        self.most_common_number = most_common_number
